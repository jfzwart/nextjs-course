import React from 'react';
import { useRouter } from 'next/router';

const EventDetailPage = () => {
    const router = useRouter();

    return (
        <div>
            <h1>The Event Detail</h1>
        </div>
    );
};

export default EventDetailPage;